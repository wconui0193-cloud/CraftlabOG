# Plain HTML/CSS/JS Scroll Showcase

This is a single-file template with:

- no dependencies
- sticky scroll scene
- manual scroll progress via `requestAnimationFrame`
- CSS variable `--progress`
- CSS-only app mockup
- transform/opacity animation
- responsive fallback
- reduced-motion fallback

Open `index.html` in a browser.

